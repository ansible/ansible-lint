---
collections:
  # that collection is used during ansible-lint testing for validating our
  # ability to import_playbook from a collection.
  # - name: community.molecule
  # # that collection is used during ansible-lint testing for validating our
  # # ability to detect non-core modules.
  # - name: ansible.posix
  # # that collection is used during ansible-lint own testing
  # - name: community.general
  # We use URLs to avoid hitting galaxy API, which may fail:
  - name: https://galaxy.ansible.com/download/ansible-posix-1.4.0.tar.gz
  - name: https://galaxy.ansible.com/download/community-general-6.0.1.tar.gz
  - name: https://galaxy.ansible.com/download/community-molecule-0.1.0.tar.gz
