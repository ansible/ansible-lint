---
cluster_info:
  query: >-
    .clusters | select(. != null) | {
      name: ("cluster_info:" + (.moid | tostring)),
      canonical_facts: {
        module: "cluster_info",
        moid: .moid
      },
      facts: {
        device_type: "cluster"
      }
    }
