language: python
python:
  - "2.7"
install:
  - pip install -r test-deps.txt
script:
  - mkdir -p dist
  - python2 setup.py --version
  - python2 setup.py egg_info
  - python2 setup.py sdist
  - pip2 wheel -w dist .
  - tox
